# creates a nametag and chat name for this player UUID... if they exist in current players...
# we gon assume that's true for now...

# add chat name...
$function chat:add_chat_nick with storage mb:data game.current_players[{UUID:$(UUID)}]

# remove nametag...
function nicks:remove_nametag

# add nametag...
$function nicks:add_nametag with storage mb:data game.current_players[{UUID:$(UUID)}]
function nicks:hide_nametag {hide:false}