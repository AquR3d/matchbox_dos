# make them on team betas (spectators)
team join betas @s
# clear inventory
clear @s
# make them spectator
gamemode spectator @s
# this is technically not necessary im realizing but that's for future daniel to get rid ofs
scoreboard players set @s alive 0
