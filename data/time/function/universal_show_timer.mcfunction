# this hides a timer by setting its visibility to false.

# used the $(id) as the macro for the timer.
# and the $(show) macro as true or false

$bossbar set $(id) visible $(show)

# yeah that's it...